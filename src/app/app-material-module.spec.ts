import { AppMaterialModule } from './app-material-module';

describe('AppMaterialModule', () => {
  it('should create an instance', () => {
    expect(new AppMaterialModule()).toBeTruthy();
  });
});
